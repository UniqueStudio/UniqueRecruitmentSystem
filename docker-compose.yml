version: "3"
services:
  dashboard:
    build: .
    environment:
    - NODE_ENV=production
  nginx:
    build:
      context: nginx
    ports:
    - 7341:7341
    volumes:
    - ./build:/usr/share/nginx/dashboard
