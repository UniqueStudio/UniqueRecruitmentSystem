export enum Env {
    dev = 'development',
    prod = 'production',
    test = 'test',
}

export enum Group {
    web = 'web',
    lab = 'lab',
    ai = 'ai',
    game = 'game',
    android = 'android',
    ios = 'ios',
    design = 'design',
    pm = 'pm',
}

export enum GroupOrTeam {
    web = 'Web',
    lab = 'Lab',
    ai = 'AI',
    game = 'Game',
    android = 'Android',
    ios = 'iOS',
    design = 'Design',
    pm = 'PM',
    unique = 'Unique',
}

export enum Period {
    morning = 'morning',
    afternoon = 'afternoon',
    evening = 'evening',
}

export enum Step {
    zeroth,
    first,
    second,
    third,
    fourth,
    fifth,
}

export enum Gender {
    other,
    male,
    female,
}

export enum Grade {
    freshman,
    sophomore,
    junior,
    senior,
    graduate,
}

export enum Rank {
    unknown,
    A,
    B,
    C,
    D,
}

export enum Evaluation {
    poor,
    fair,
    good,
}
