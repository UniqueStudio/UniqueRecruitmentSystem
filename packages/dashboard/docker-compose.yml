version: "3"
services:
  dashboard:
    build:
      context: .
    environment:
    - NODE_ENV=production
  nginx:
    build:
      context: nginx
    ports:
    - 7341:7341
    volumes:
    - ./build:/usr/share/nginx/dashboard
    - /etc/pki/tls:/etc/nginx/certs

